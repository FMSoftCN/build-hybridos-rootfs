diff --git a/CMakeLists.txt b/CMakeLists.txt
index 451e7a6..c6a6ec4 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -49,11 +49,6 @@ else()
     endif()
 endif()
 
-# Conditionally build bindings
-if(NOT WIN32)
-    add_subdirectory(bindings)
-endif()
-
 # Always build these
 add_subdirectory(libproxy)
 add_subdirectory(utils)
